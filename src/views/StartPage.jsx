const React = require('react');
const Layout = require('./Layout');

module.exports = function StartPage() {
  return (
    <Layout>
      <div>
        <h1>I'mho</h1>
      </div>
    </Layout>
  );
};
